apiVersion: datadoghq.com/v2alpha1
kind: DatadogAgent
metadata:
  name: datadog-agent-rc-updater
  labels:
    agent.datadoghq.com/e2e-test: datadog-agent-rc
spec:
  features:
    cws:
      enabled: false
    usm:
      enabled: false
    cspm:
      enabled: false
    sbom:
      enabled: false
  # TODO: global.clusterName is ignored by the provider
  # global:
  #   clusterName: rc-updater-e2e-test-cluster
