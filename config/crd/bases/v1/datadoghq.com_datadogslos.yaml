
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.6.1
  creationTimestamp: null
  name: datadogslos.datadoghq.com
spec:
  group: datadoghq.com
  names:
    kind: DatadogSLO
    listKind: DatadogSLOList
    plural: datadogslos
    singular: datadogslo
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.id
      name: id
      type: string
    - jsonPath: .status.syncStatus
      name: sync status
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DatadogSLO allows to define and manage datadog SLOs from kubernetes
          cluster
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            properties:
              description:
                description: Description of this service level objective.
                type: string
              groups:
                description: Groups A list of (up to 100) monitor groups that narrow
                  the scope of a monitor service level objective. Included in service
                  level objective responses if it is not empty. Optional in create/update
                  requests for monitor service level objectives, but may only be used
                  when then length of the monitor_ids field is one.
                items:
                  type: string
                type: array
              monitor_ids:
                description: MonitorIDs a list of monitor IDs that defines the scope
                  of a monitor service level objective. Required if type is monitor.
                items:
                  format: int64
                  type: integer
                type: array
              name:
                description: Name of Datadog service level objective
                type: string
              query:
                description: Query The metric query of good / total events
                properties:
                  denominator:
                    description: Denominator the sum of the total events.
                    type: string
                  numerator:
                    description: Numerator the sum of all the good events.
                    type: string
                required:
                - denominator
                - numerator
                type: object
              tags:
                description: 'Tags A list of tags to associate with your service level
                  objective. This can help you categorize and filter service level
                  objectives in the service level objectives page of the UI. Note:
                  it''s not currently possible to filter by these tags when querying
                  via the API'
                items:
                  type: string
                type: array
              thresholds:
                description: Thresholds A list of thresholds and targets that define
                  the service level objectives from the provided SLIs.
                items:
                  properties:
                    target:
                      anyOf:
                      - type: integer
                      - type: string
                      description: The target value for the service level indicator
                        within the corresponding timeframe.
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    target_display:
                      description: TargetDisplay A string representation of the target
                        that indicates its precision. It uses trailing zeros to show
                        significant decimal places (for example 98.00). Always included
                        in service level objective responses. Ignored in create/update
                        requests.
                      type: string
                    timeframe:
                      description: 'Timeframe The SLO time window options. Allowed
                        enum values: 7d,30d,90d,custom'
                      type: string
                    warning:
                      anyOf:
                      - type: integer
                      - type: string
                      description: The warning value for the service level objective.
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    warning_display:
                      description: A string representation of the warning target (see
                        the description of the target_display field for details).
                        Included in service level objective responses if a warning
                        target exists. Ignored in create/update requests.
                      type: string
                  required:
                  - target
                  - timeframe
                  type: object
                type: array
              type:
                description: 'Type the slo allowed values: monitor | metric'
                type: string
            required:
            - name
            - query
            - thresholds
            - type
            type: object
          status:
            description: DatadogSLOStatus defines the observed state of DatadogSLO
            properties:
              conditions:
                description: Conditions Represents the latest available observations
                  of a DatadogSLOs current state.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource. --- This struct is intended for direct
                    use as an array at the field path .status.conditions.  For example,
                    type FooStatus struct{     // Represents the observations of a
                    foo's current state.     // Known .status.conditions.type are:
                    \"Available\", \"Progressing\", and \"Degraded\"     // +patchMergeKey=type
                    \    // +patchStrategy=merge     // +listType=map     // +listMapKey=type
                    \    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`
                    \n     // other fields }"
                  properties:
                    lastTransitionTime:
                      description: lastTransitionTime is the last time the condition
                        transitioned from one status to another. This should be when
                        the underlying condition changed.  If that is not known, then
                        using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: message is a human readable message indicating
                        details about the transition. This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: observedGeneration represents the .metadata.generation
                        that the condition was set based upon. For instance, if .metadata.generation
                        is currently 12, but the .status.conditions[x].observedGeneration
                        is 9, the condition is out of date with respect to the current
                        state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: reason contains a programmatic identifier indicating
                        the reason for the condition's last transition. Producers
                        of specific condition types may define expected values and
                        meanings for this field, and whether the values are considered
                        a guaranteed API. The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                        --- Many .condition.type values are consistent across resources
                        like Available, but because arbitrary conditions can be useful
                        (see .node.status.conditions), the ability to deconflict is
                        important. The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              created:
                description: Created is the time the SLO was created
                format: date-time
                type: string
              creator:
                description: Creator is the identity of the SLO creator
                type: string
              currentHash:
                description: CurrentHash tracks the hash of the current DatadogMonitorSpec
                  to know if the Spec has changed and needs an update
                type: string
              id:
                description: ID is the SLO ID generated in Datadog
                type: string
              primary:
                description: ManagedByDatadogOperator defines whether the SLO is managed
                  by the Kubernetes custom resource (true) or outside Kubernetes (false)
                type: boolean
              syncStatus:
                description: SyncStatus shows the health of syncing the SLO state
                  to Datadog
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
