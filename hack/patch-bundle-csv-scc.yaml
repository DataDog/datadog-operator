spec:
  install:
    spec:
      clusterPermissions:
      - rules:
        - apiGroups:
          - security.openshift.io
          resourceNames:
          - hostaccess
          - privileged
          resources:
          - securitycontextconstraints
          verbs:
          - use
        serviceAccountName: datadog-agent-scc
