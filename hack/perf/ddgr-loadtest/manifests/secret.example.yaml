# hack/perf/ddgr-loadtest/manifests/secret.example.yaml
# Example only — DO NOT commit real keys.
# Create the real secret with:
#   kubectl create secret generic sandbox-datadog-creds \
#     --from-literal=api-key=$DD_API_KEY \
#     --from-literal=app-key=$DD_APP_KEY \
#     -n system
apiVersion: v1
kind: Secret
metadata:
  name: sandbox-datadog-creds
  namespace: system
type: Opaque
stringData:
  api-key: REPLACE_ME
  app-key: REPLACE_ME
